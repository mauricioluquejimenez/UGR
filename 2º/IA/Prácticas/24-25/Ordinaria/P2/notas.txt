- Nivel 1
    - ¿Hay que modificar el método VeoCasillaInteresanteR para incluir las casillas de tipo sendero como iguales a las casillas de tipo camino?¿O se hace una función específica para el nivel 1?
    - Uso de la función SituarSensorEnMapaR justo debajo de la línea "Action accion;"
    - ¿Cambio de heurística para maximizar el conocimiento del mapa?

    // Cambios respecto al nivel 0

    int ComportamientoRescatador::VeoCasillaInteresanteR_Nivel_1(char i, char c, char d, bool zap)
    {
        if (c == 'X') return 2;
        else if (i == 'X') return 1;
        else if (d == 'X') return 3;
        else if (!zap)
        {
            if 		(c == 'D') return 2;
            else if (i == 'D') return 1;
            else if (d == 'D') return 3;
        }
        if 		(c == 'C') or (c == 'S') return 2;
        else if (i == 'C') or (c == 'S') return 1;
        else if (d == 'C') or (c == 'S') return 3;
        else return 0;
    }